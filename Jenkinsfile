pipeline {
  agent any
  stages {
    stage('init') {
      steps {
        echo "tesing..."
    }}
    stage('build') {
      steps {
        echo "tesing..."
    }}
    stage('deploy') {
      steps {
        echo "tesing..."
    }}
  }
}
