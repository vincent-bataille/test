pipeline {
  agent any
  stages {
    stage('init') {
      steps {
        echo "tesing..."
    }}
    stage('build') {
      steps {
        echo "building..."
    }}
    stage('deploy') {
      steps {
        echo "deploying..."
    }}
  }
}
